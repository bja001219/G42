rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // G42 게임 방.
    // ⚠ 데모/개발용 개방형 규칙. 운영 전에는 Firebase Auth + 방 멤버 검증으로 강화할 것.
    match /rooms/{code} {
      allow read, write: if true;
    }
  }
}
